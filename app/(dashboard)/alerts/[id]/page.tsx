import { ArrowLeft, CalendarDays, Gauge, PlaneTakeoff } from "lucide-react";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getDashboardData } from "@/src/modules/alerts/queries";
import { formatPrice } from "@/src/shared/lib/utils";
export default async function AlertDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const { alerts } = await getDashboardData(); const alert = alerts.find((item) => item.id === id); if (!alert) notFound(); return <main className="form-page"><header className="form-header"><Link className="icon-button" href="/"><ArrowLeft size={19} /></Link><div><p className="eyebrow">SZCZEGÓŁY ALERTU</p><h1>{alert.origin} → {alert.destination}</h1></div></header><section className="detail-hero"><PlaneTakeoff size={26} /><p>Monitorujemy tę trasę raz dziennie i połączymy wszystkie znalezione okazje w jedno powiadomienie.</p></section><div className="detail-grid"><section className="settings-card"><CalendarDays /><div><h2>{alert.departureDate}{alert.returnDate && ` – ${alert.returnDate}`}</h2><p>{alert.flexDays ? `Elastyczność ±${alert.flexDays} dni` : "Dokładne daty"}</p></div></section><section className="settings-card"><Gauge /><div><h2>Limit {formatPrice(alert.maxPrice)}</h2><p>Najlepsza cena: {alert.bestPrice ? formatPrice(alert.bestPrice) : "jeszcze szukamy"}</p></div></section></div></main>; }